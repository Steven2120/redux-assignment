import React from "react";
import { Box } from "@mui/system";

const Footer = (props) => {
  return (
    <>
      <Box sx={{ p: 6, background: "blue" }}>You are viewing the Footer</Box>
    </>
  );
};

export default Footer;
